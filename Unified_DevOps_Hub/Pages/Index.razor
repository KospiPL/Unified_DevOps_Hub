@page "/"

<div class="main-container">
    <div class="login-form">
        <h2>Logowanie</h2>
        <EditForm Model="loginModel">
            <InputText id="username" @bind-Value="loginModel.Username" placeholder="Nazwa użytkownika" />
            <InputText type="password" id="password" @bind-Value="loginModel.Password" placeholder="Hasło" />
            <button type="submit">Zaloguj się</button>
        </EditForm>
    </div>
</div>

<style>
    .main-container {
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
        background: url("C:\Users\KacperCudzik\Downloads\DALL·E 2023-12-29 10.55.33 - Create a simple, sleek logo icon for 'Unified DevOps Hub' similar to the previously generated image but without any background or additional objects. .png");
    }

    .login-form {
        padding: 20px;
        background: white;
        border-radius: 10px;
        box-shadow: 0 5px 15px rgba(0, 0, 0, 0.3);
    }

        .login-form h2 {
            color: #333;
            margin-bottom: 20px;
        }

    input {
        width: 100%;
        padding: 10px;
        margin-bottom: 10px;
        border: none;
        border-bottom: 2px solid #ccc;
        font-size: 16px;
    }

    button {
        width: 100%;
        padding: 10px;
        background-color: #007bff;
        border: none;
        border-radius: 5px;
        color: white;
        font-size: 18px;
        cursor: pointer;
        transition: background-color 0.3s;
    }

        button:hover {
            background-color: #0056b3;
        }
</style>

@code {
    private LoginModel loginModel = new LoginModel();

    public class LoginModel
    {
        public string Username { get; set; }
        public string Password { get; set; }
    }

   
    private void HandleLogin()
    {
        // Logowanie...
    }
}
